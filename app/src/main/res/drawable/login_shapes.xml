<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <size
        android:width="24dp"
        android:height="24dp" />

    <corners
        android:bottomLeftRadius="@dimen/login_shape_radius"
        android:bottomRightRadius="@dimen/login_shape_radius"
        android:topLeftRadius="@dimen/login_shape_radius"
        android:topRightRadius="@dimen/login_shape_radius" />
    <solid android:color="@color/colorPrimary" />

</shape>