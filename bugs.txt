Some unsynced data occasionally happen, usually missing 1 or 2 sections (eg: Refraction, Ocular Health), will only show up after full reload
Beberapa data yang tidak disinkronkan kadang-kadang terjadi, biasanya hilang 1 atau 2 bagian (misalnya: Refraksi, Kesehatan Mata), hanya akan muncul setelah reload penuh

Occasional crash after taking photo, usually happens after the tablet is not used for sometime (with the app stay open)
Sesekali crash setelah mengambil foto, biasanya terjadi setelah tablet tidak digunakan untuk beberapa waktu (dengan aplikasi tetap terbuka)

Unable to change ID unless the name is edited as well
Tidak dapat mengubah ID kecuali namanya juga diedit
DONE

Changing Patient ID to already used ID will trigger a prompt saying ID already taken, but the app still merge them together
Mengubah ID Pasien menjadi ID yang sudah digunakan akan memicu prompt yang mengatakan ID sudah diambil, tetapi aplikasi masih menggabungkannya
DONE

kgopto